import random
a = 2
ent = int(ent)
rand = random.randint(1,100)
print(rand)
def game_random(rand):
    a = True
    while a :
        if ent=="b":
            if rand <=0 and
            print("mine is larger")
        elif ent<rand:
            print("mine is smaller")
        elif ent == rand:
            print("you are roke")
        if
        rand = random.randint(1, 100)
        print(rand)
        ent = input("Enter your letter : ")
while a>0:
    game_random(ent)

    write = input("are you ready(y,n) : ")
    if write == "n":
        break
    elif write=="y":
        a += 1
    rand = random.randint(1, 100)

